# Деплой приложения MOST

## ✅ Готовность к деплою

Приложение готово к деплою! Все проверки пройдены:

- ✅ Сборка проходит успешно (`npm run build`)
- ✅ Нет ошибок линтера
- ✅ Структура проекта чистая и организована
- ✅ Все зависимости установлены

## Варианты деплоя

### 1. **Vercel** (Рекомендуется) ⭐

**Плюсы:**

- Автоматический деплой из GitHub
- Бесплатный хостинг
- Быстрая работа
- Поддержка SPA routing

**Как деплоить:**

1. Установите Vercel CLI:

```bash
npm i -g vercel
```

2. Войдите в Vercel:

```bash
vercel login
```

3. Деплой:

```bash
vercel
```

**Или через веб-интерфейс:**

1. Зайдите на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Настройки:
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

**Настройка SPA routing:**
Создайте файл `vercel.json` в корне проекта:

```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

---

### 2. **Netlify**

**Плюсы:**

- Простой деплой через drag & drop
- Автоматический деплой из GitHub
- Бесплатный хостинг

**Как деплоить:**

1. Установите Netlify CLI:

```bash
npm install -g netlify-cli
```

2. Деплой:

```bash
netlify deploy --prod --dir=dist
```

**Или через веб-интерфейс:**

1. Зайдите на [netlify.com](https://netlify.com)
2. Перетащите папку `dist` на страницу деплоя
3. Или подключите GitHub репозиторий

**Настройка SPA routing:**
Создайте файл `public/_redirects`:

```
/*    /index.html   200
```

---

### 3. **Firebase Hosting** (Рекомендуется для Firebase проектов)

**Плюсы:**

- Интеграция с Firebase
- Бесплатный хостинг
- Простой деплой

**Как деплоить:**

1. Установите Firebase CLI:

```bash
npm install -g firebase-tools
```

2. Войдите в Firebase:

```bash
firebase login
```

3. Инициализируйте проект:

```bash
firebase init hosting
```

Выберите:

- Use an existing project (выберите ваш Firebase проект)
- What do you want to use as your public directory? `dist`
- Configure as a single-page app? `Yes`
- Set up automatic builds? `No` (можно настроить позже)

4. Создайте файл `firebase.json` (если его нет):

```json
{
  "hosting": {
    "public": "dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

5. Деплой:

```bash
npm run build
firebase deploy --only hosting
```

---

### 4. **GitHub Pages**

**Плюсы:**

- Бесплатно
- Интеграция с GitHub

**Как деплоить:**

1. Установите `gh-pages`:

```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:

```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://yourusername.github.io/most"
}
```

3. Обновите `vite.config.js`:

```js
export default defineConfig({
  plugins: [react()],
  base: "/most/", // имя вашего репозитория
});
```

4. Деплой:

```bash
npm run deploy
```

---

## 📋 Чек-лист перед деплоем

- [x] Сборка проходит успешно
- [x] Линтер не выдает ошибок
- [x] Firebase конфигурация настроена
- [ ] Проверьте, что Firebase проект настроен правильно
- [ ] Убедитесь, что правила безопасности Firestore настроены
- [ ] Проверьте, что коллекция `admins` существует и содержит админов

## 🔐 Переменные окружения (.env)

**`.env` файл опционален**, но поддерживается для гибкости работы с разными окружениями.

### Текущая реализация:

Firebase конфигурация (`src/config/firebase.js`) поддерживает переменные окружения с fallback на значения по умолчанию:

```javascript
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY || "значение-по-умолчанию",
  // ... остальные параметры
};
```

### Как использовать:

1. **Без `.env` файла (текущий вариант):**

   - Приложение использует значения по умолчанию из `firebase.js`
   - Работает сразу, без дополнительной настройки ✅

2. **С `.env` файлом (для разных окружений):**
   - Создайте файл `.env` в корне проекта
   - Добавьте переменные с префиксом `VITE_`:
     ```env
     VITE_FIREBASE_API_KEY=your-api-key
     VITE_FIREBASE_AUTH_DOMAIN=your-auth-domain
     VITE_FIREBASE_PROJECT_ID=your-project-id
     VITE_FIREBASE_STORAGE_BUCKET=your-storage-bucket
     VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
     VITE_FIREBASE_APP_ID=your-app-id
     VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id
     ```
   - `.env` уже добавлен в `.gitignore`

**Важно:**

- В Vite переменные окружения должны иметь префикс `VITE_`
- Firebase ключи для веб-приложений публичные, но `.env` упрощает работу с dev/prod окружениями

## 🔧 Настройки после деплоя

1. **Firebase Rules:**
   Убедитесь, что правила Firestore настроены для продакшена:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для категорий
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if false; // Только через админку
    }

    // Правила для вопросов
    match /questions/{questionId} {
      allow read: if true;
      allow write: if false; // Только через админку
    }

    // Правила для голосов
    match /votes/{voteId} {
      allow read, write: if true; // Пользователи могут голосовать
    }

    // Правила для пользователей
    match /users/{userId} {
      allow read, write: if true;
    }

    // Правила для админов
    match /admins/{email} {
      allow read: if false; // Только для проверки в сервисе
    }
  }
}
```

2. **Добавьте домен в Telegram Bot:**
   После деплоя добавьте URL в настройки вашего Telegram бота.

3. **CORS настройки:**
   Если нужно, настройте CORS в Firebase для вашего домена.

## 🚀 Быстрый старт

**Рекомендуемый способ (Vercel):**

```bash
npm i -g vercel
vercel
```

Или через GitHub:

1. Загрузите код в GitHub
2. Зайдите на vercel.com
3. Подключите репозиторий
4. Деплой произойдет автоматически!
